#!/bin/bash
set -e

# Automatically added by dh_makeshlibs
if [ "$1" = "configure" ]; then
	ldconfig
fi
# End automatically added section

if ! [ -a /etc/openisr/hostid ]; then

	dd if=/dev/urandom bs=4 count=1 status=noxfer | od -An -t d4 | tr -d " -" > /etc/openisr/hostid

fi
