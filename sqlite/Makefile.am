# Suppress build warnings, since we're not in a position to fix them
AM_CFLAGS = -w
AM_LDFLAGS = -lpthread -ldl -release 3.5.9
pkglib_LTLIBRARIES = libsqlite.la
libsqlite_la_SOURCES = sqlite3.c sqlite3.h
